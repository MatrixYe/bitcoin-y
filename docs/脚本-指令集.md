# 脚本指令集
// todo 添加文档

## StackOp:栈机操作
`[x0,x1,x2,x3,x4]`表示栈，其中最右侧为栈顶。

## ToAltStack
将主栈栈顶移动到备用栈的栈顶
```text
主栈：[x0,x1,x2,x3,x4] => [x0,x1,x2,x3]
备用栈：[] => [x4]
```

## FromAltStack
将备用栈的栈顶移动到主栈栈顶
```text
主栈：[x0,x1,x2,x3] => [x0,x1,x2,x3,x4]
备用栈：[x4] => []
```

