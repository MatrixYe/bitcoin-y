## 自定义编程规范


## 函数命名
动词开头，蛇形命名。
- 普通的单一求值用 `get_*`
- 复杂的多步骤求值用 `compute_*`
- 数字转化类型计算用 `to_*`
- 累加、求和、统计类专用词 `sum_*`,`count_*`,`avg_*`

## 结构体构造函数
- 默认使用 `new()`
- 从已有类型转化而来，使用`from_*`
- 从已有的字段类型转化而来，链式调用 `with_*`
- 通过解析而来，使用`parse_from_*`
- 批量生产结构体，使用`batch_*`
- 

